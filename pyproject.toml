[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quiet-uk"
version = "0.2.0"
description = "Starter pipeline for UK environmental noise mapping"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
